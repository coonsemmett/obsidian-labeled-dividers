---
type: archetype
project: ob-labeled-dividers
updated: 2026-08-14
---
# ob-labeled-dividers — Project Archetype

Obsidian plugin — labeled dividers.

## Federal law (every code project — ratified 2026-08-14)
- **Two Worlds Rule:** notes live in the vault (26V); code lives here in `Code Projects/`.
- This project has a private GitHub repo — the live truth for commit history: https://github.com/coonsemmett/obsidian-labeled-dividers.git
- **Default is COMMIT + PUSH before ending any working session.** Not committing requires a dated, named reason written into `_admin/GIT.md`. Silence is not an option.
- Agents and subagents inherit this rule. The session that does the work commits the work.

## This project
- Git page: `_admin/GIT.md` — GitHub link, policy, named no-commit reasons.
- HTML/explanatory artifacts route to `_admin/resources/html/` (vault rule: html-routing).
- `_admin/` is the folder ABOUT the project (meta layer); the project's real work lives outside it.

## Maintenance
This file is the single canonical context file. `CLAUDE.md` and `AGENTS.md` are permanent one-line adapter stubs pointing here — never edit the stubs; edit THIS file. (Philosophy: Project Federal Archetype Rule.)
